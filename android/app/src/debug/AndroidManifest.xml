<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <!-- Permiso para Internet (usado por Flutter) -->
  <uses-permission android:name="android.permission.INTERNET"/>

  <!-- No es necesario duplicar los permisos aquí, solo mantener los básicos -->
  <application
    android:requestLegacyExternalStorage="true"
    android:label="guita"
    android:icon="@mipmap/ic_launcher">
    <!-- Otras configuraciones de la app de depuración -->
  </application>
</manifest>
